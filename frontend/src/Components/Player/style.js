import styled from "styled-components";

export const PlayerWrapper = styled.div`
  /* Player ratio: 100 / (1280 / 720) */
`;

export const LocalPlay = styled.h1`
  font-size: 60px;
  color: black;
  text-align: center;
  z-index: 5555;
`;
